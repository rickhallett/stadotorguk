---
---

<section class="hero">
  <div class="hero-content">
    <h1 class="hero-title">DEMANDING ACCOUNTABILITY</h1>
    <p class="hero-subtitle">The Democratic Process Has Been Warped For Personal Gain</p>
    <p class="hero-description">
      We are collecting evidence to show how our voices have been ignored and how data has been 
      manipulated to serve personal interests. Join us in demanding transparency.
    </p>
    <hr class="hero-rule" />
  </div>
</section>

<style>
.hero {
  padding: calc(var(--grid-unit) * 10) 0;
  text-align: center;
  border-bottom: 1px solid var(--color-border);
}

.hero-content {
  max-width: 800px;
  margin: 0 auto;
}

.hero-title {
  font-family: var(--font-heading-primary);
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 900;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: var(--color-accent);
  margin-bottom: calc(var(--grid-unit) * 3);
  line-height: 1.1;
}

.hero-subtitle {
  font-family: var(--font-heading-secondary);
  font-size: clamp(1.2rem, 3vw, 1.8rem);
  font-weight: 300;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--color-text-primary);
  margin-bottom: calc(var(--grid-unit) * 3);
  line-height: 1.3;
}

.hero-description {
  font-family: var(--font-body);
  font-size: 1.1rem;
  line-height: 1.8;
  color: var(--color-text-secondary);
  margin-bottom: calc(var(--grid-unit) * 4);
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}

.hero-rule {
  width: 100%;
  max-width: 150px;
  height: 6px;
  background-color: var(--color-accent);
  border: none;
  margin: calc(var(--grid-unit) * 4) auto;
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .hero {
    padding: calc(var(--grid-unit) * 6) 0;
  }
}
</style>